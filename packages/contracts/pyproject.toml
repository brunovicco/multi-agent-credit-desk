[project]
name = "credit-desk-contracts"
version = "0.1.0"
description = "Shared schemas and contracts for the credit desk workspace. No runtime dependencies."
readme = "README.md"
requires-python = ">=3.13,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/credit_desk_contracts"]
